# Imagen del panel de auth

El template Angular referencia `src/assets/images/auth-screens.png`, pero **ese archivo no existe en el repositorio Angular**.

Para el login visualmente equivalente, coloca aquí la captura del dashboard:

```
htmx-export/assets/images/auth-screens.png
```

Ruta servida en Go: `/static/assets/images/auth-screens.png`

Alternativa temporal: usar cualquier screenshot del dashboard admin en PNG (~1200×800).
